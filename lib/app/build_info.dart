// ═══════════════════════════════════════════════════════════════════
//  build_info.dart — 이 빌드가 언제 만들어졌는가
// ═══════════════════════════════════════════════════════════════════
//
//  주요 기능 : 빌드 시각을 문자열 하나로 들고 있는다 (개발자 패널이 띄운다)
//  제외 사항 : 버전 번호·git 정보 (필요해지면 그때 늘린다)
//
//  ⭐ 상세 설명 : **화면에 보이는 것이 방금 고친 코드인지 확인하는 용도**다.
//    같은 시험을 반복하다 보면 「이게 새 코드가 맞나, 브라우저가 옛것을 물고 있나」가
//    의심되는데, 눈으로 확인할 방법이 없으면 시험 결과 자체를 믿을 수 없다.
//
//  ⭐ 주요 로직 : 값은 **빌드할 때 밖에서 넣는다**(`--dart-define=BUILD_AT=...`).
//    Dart 코드는 실행 중에 소스 파일의 수정 시각을 볼 수 없다.
//    넣지 않고 그냥 실행하면 `—` 로 뜬다 — 그것도 정보다(스크립트를 안 썼다는 뜻).
//
//  ⭐ 주요 로직 : 주입값이 매번 달라지므로 **빌드가 새로 돌아간다.**
//    브라우저가 옛것을 물고 있을 여지가 그만큼 줄어드는 것이 덤이다.
//
//  쓰는 법 : `.\run.ps1` (프로젝트 루트) — 시각을 넣어 크롬으로 띄운다
//
// ═══════════════════════════════════════════════════════════════════

/// 빌드 시각. `--dart-define=BUILD_AT=...` 로 넣는다
const String buildAt = String.fromEnvironment('BUILD_AT', defaultValue: '—');
